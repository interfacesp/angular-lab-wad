import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cultureProgrWad22',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
